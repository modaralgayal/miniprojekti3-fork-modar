name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:latest
        env:
          POSTGRES_DB: test
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
        ports:
          - 5432:5432
    
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install Poetry
        run: pip install poetry
      - name: Install dependencies
        run: poetry install
      - name: Run tests with production environment variable
        run: |
          export production=test
          export DATABASE_URL="postgresql://test:test@localhost:5432"
          poetry run pytest

          # Check if the environment is set to test
          if [ "$production" == "test" ]; then
            # Modify your Python code accordingly
            sed -i 's|db_address = "postgresql://.*"|db_address = "postgresql:///testi"|g' your_script.py
          fi
